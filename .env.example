# Postgres connection string (Vercel Postgres / Neon / Supabase all work).
# Vercel injects this automatically once you connect a database to the project.
DATABASE_URL=

# Optional: email a copy of each contact submission via Resend.
RESEND_API_KEY=
CONTACT_TO_EMAIL=
CONTACT_FROM_EMAIL=

# Password for the /admin dashboard (minimum 8 characters).
ADMIN_PASSWORD=
